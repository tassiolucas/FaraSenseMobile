<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FaraSenseToolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:minHeight">?attr/actionBarSize</item>
        <item name="titleMargin">0dp</item>
        <item name="contentInsetStart">0dp</item>
    </style>

    <style name="FaraSenseToolbarTitle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/title_toolbar</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="FaraSenseToolbarSubtitle" parent="FaraSenseToolbarTitle">
        <item name="android:textSize">@dimen/subttitle_toolbar</item>
    </style>

    <style name="FaraSenseCardView" parent="CardView">
        <item name="cardUseCompatPadding">true</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/card_view_half_margin</item>
        <item name="android:layout_marginRight">@dimen/card_view_half_margin</item>
    </style>

    <style name="ChartCardView" parent="FaraSenseCardView">
        <item name="android:clickable">false</item>
        <item name="cardElevation">3dp</item>
        <item name="cardCornerRadius">10dp</item>
    </style>

    <style name="FaraSenseTitleChart">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="FaraSenseEditText" >
        <item name="colorControlNormal">@color/colorLogo</item>
        <item name="colorControlActivated">@color/colorTabBar</item>
        <item name="colorControlHighlight">@color/colorLogo</item>
    </style>

</resources>
